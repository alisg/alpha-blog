<nav class="navbar navbar-expand-xl bg-transparent" >
    <%# <a class="navbar-brand" href="#" id="logo">Alpha Blog</a> %>
    <%= link_to "ALPHA BLOG", root_path, class: 'navbar-brand', id: 'logo' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent" >
        <ul class="nav justify-content-end" >
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Articles
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdow">
                    <% if logged_in? %>
                        <%= link_to "Create New Article", new_article_path, class: "dropdown-item"%>
                    <% end %>
                    <%= link_to "View Articles", articles_path, class: "dropdown-item"%>
                </div>
            </li>
            <li class="nav-item">
                <%= link_to "Bloggers", users_path, class: "nav-link"%>
            </li>
            <% if logged_in?%>
                <%# <li class="nav-item">
                    <%= link_to current_user.username, user_path(current_user), class: "nav-link active" %>
                <%# </li> %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <%= "(Admin)" if current_user.admin?  %> Profile [<%= current_user.username %>]
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdow">
                        <%= link_to "View your profile", user_path(current_user), class: "dropdown-item"%>
                        <%= link_to "Edit your profile", edit_user_path(current_user), class: "dropdown-item"%>
                        <%= link_to "Delete profile", user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: { confirm: "Are you sure?"}%>
                    </div>
                </li>
                <li class="nav-item">
                    <%= link_to "Log Out", logout_path, class: "nav-link active", method: :delete %>
                </li>
            <% else%>
                <li class="nav-item">
                    <%= link_to "Log In", login_path, class: "nav-link active" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Sign Up", signup_path, class: "nav-link active" %>
                </li>
            <% end %>
        </ul>
    </div>

    <form class="form-inline my-2 my-sm-0">
        <input class="form-control mr-sm-2" type="search" aria-label="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit" style="color: white; border-color: beige;" >Search</button>
    </form>
</nav>